In data folder there is zipped processed data.
In results folder there are results from running the code so far.
The code is in src folder.

To run the code packages from requirements.txt have to be installed. (pip install -r requirements.txt)
